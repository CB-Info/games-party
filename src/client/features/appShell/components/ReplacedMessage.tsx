/** Shown instead of the content once another tab took the session over (docs/architecture.md, §4). */
export function ReplacedMessage() {
  return <p className="t-title-2">Games Party est ouvert dans un autre onglet.</p>;
}
