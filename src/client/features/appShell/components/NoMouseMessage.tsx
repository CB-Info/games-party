/** Shown instead of the content on a touch screen with no mouse (docs/architecture.md, §2). */
export function NoMouseMessage() {
  return <p className="t-title-2">Games Party se joue sur ordinateur, avec une souris.</p>;
}
